apply plugin: 'com.android.application'

android {
    signingConfigs {
        mikey {
            keyAlias 'AAL'
            keyPassword 'KEY2016rcp'
            storeFile file('C:/Users/RobertoPrado/Desktop/Key Android/mikey.jks')
            storePassword 'KEY2016rcp'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.3"
    defaultConfig {
        applicationId "com.aal.epn.servlets_backend"
        minSdkVersion 16
        targetSdkVersion 19
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.0.0'
    compile 'it.sephiroth.android.library.picasso:picasso:2.5.2.4b'
}
