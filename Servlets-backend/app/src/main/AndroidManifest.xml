<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aal.epn.servlets_backend">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_feeding"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".almuerzoActivity" />
        <activity android:name=".beneficioActivity" />
        <activity android:name=".almuerzosActivity" />
        <activity android:name=".semanaActivity" />
        <activity android:name=".InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".IMC"
            android:label="@string/title_activity_imc"
            android:theme="@style/AppTheme" />
        <activity android:name=".IndiceCinturaCadera"></activity>
    </application>

</manifest>